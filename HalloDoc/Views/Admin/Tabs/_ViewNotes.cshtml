@*@using DataAccess.ViewModel;
@model AdminDashboard
<div class="container">    <div class="d-flex justify-content-between mt-4">        <div class="d-flex ">            <h3 class="m-0 pt-2 fs-5 ">Notes</h3>        </div>        <button type="button" class="btn btn-outline-info p-2 pe-3 button-info" onclick="location.href='@Url.Action("DashboardHeader", "Admin")'">            <i class="bi bi-chevron-left i-info"></i>Back        </button>    </div>    <div class="">        <div class="row g-2 my-4">            <div class="col-md me-4 d-flex shadow rounded p-3">                <div class="m-auto text-secondary col-1"><i class="bi bi-arrow-left-right text-secondary"></i></div>                <div class="ps-2 col-11">                    <div class="fw-bold">Transfer Notes</div>                    <div class="w-100">                        <select class="w-100 py-2 px-1  border-0 " id="Region">                            @foreach (var item in Model.ViewNotesModel)
                            {
                                if (item == Model.ViewNotesModel.Last())
                                {                                    <option selected disabled>Admin Transffered to Dr. @item.PhysicianFName @item.PhysicianLName on @item.CreatedDate.Date At @item.CreatedDate.TimeOfDay : @item.TransferNotes </option>                                }
                                if (item != Model.ViewNotesModel.Last())
                                {                                    <option selected disabled>Admin Transffered to Dr. @item.PhysicianFName @item.PhysicianLName on @item.CreatedDate.Date At @item.CreatedDate.TimeOfDay : @item.TransferNotes </option>                                }
                            }                            <option class="text-secondary">No More Items</option>                        </select>                    </div>                </div>            </div>            <div class="col-md d-flex shadow rounded p-3 w-100">                <div class="m-auto text-secondary col-1"><i class="bi bi-clipboard2-pulse text-secondary"></i></div>                <div class="ms-2 col-11">                    <div class="fw-bold">Physician Notes</div>                    <div>@Model.PhysicianNotes</div>                </div>            </div>        </div>        <div class="row g-2 my-4">            <div class="col-md me-4 d-flex shadow rounded p-3 w-100">                <div class="m-auto text-secondary col-1"><i class="bi bi-person-gear text-secondary"></i></div>                <div class="ps-2 col-11">                    <div class="fw-bold">Admin Notes</div>                    <div>@Model.AdminNotes</div>                </div>            </div>            <div class="col-md">            </div>        </div>        <form method="post" action='@Url.Action("AdditionalNotesData", "Admin", new { reqid = Model.RequestId})'>            <div class="shadow rounded p-3">                <div class="form-floating my-4">                    <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea2" style="height: 100px" asp-for="@Model.AdditionalNotes" required></textarea>                    <label for="floatingTextarea2" class="text-secondary floatingLabel" id="label-floating">Admin Notes</label>                </div>                <div class="d-flex justify-content-end mt-4 mb-4">                    <button type="submit" class="btn btn-info me-2 info-button">Save Changes </button>                </div>            </div>        </form>    </div></div>*@